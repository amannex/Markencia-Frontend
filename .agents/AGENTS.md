# Workspace Agent Rules

## Git Workflow & Pushing Changes
- Always create a new git branch with a descriptive name based on the task or feature before committing changes.
- Commit all changes to that newly created branch.
- Push the new branch to the remote repository rather than pushing directly to `main`, `master`, or existing shared branches.
